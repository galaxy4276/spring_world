server:
  port:

spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:
  mail:
    host:
    port:
    username:
    password:
    properties:
      mail:
        smtp:
          auth:
          starttls:
            enable:
  jpa:
    hibernate:
      ddl-auto:
    show-sql:
